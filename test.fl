
#This is an example of a comment, notice how this doesnt get added to the token list?#

func main() returns none
    let num Αλφα = (4 + 7) * 8 - 3;
end

func otherFunc(foo bar, num glorp) returns retType
end